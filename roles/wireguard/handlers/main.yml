---
- name: Restart the WireGuard interface
  ansible.builtin.systemd_service:
    name: "wg-quick@{{ wireguard_interface }}"
    state: restarted
  when: not (ansible_check_mode and _wireguard_install is changed)
  listen: "restart wireguard"
